# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
#
# Mount the archiso boot medium before unpackfs runs.
# This ensures /run/archiso/bootmnt is available for airootfs.sfs access.
---
dontChroot: true
timeout: 30

script:
  - command: "/usr/local/bin/mount-bootmnt.sh"
    timeout: 20
