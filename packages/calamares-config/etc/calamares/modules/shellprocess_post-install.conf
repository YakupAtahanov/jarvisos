# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
#
# Post-installation shell commands
# Runs outside chroot with @@ROOT@@ prefix for target path access
---
dontChroot: true
timeout: 120

script:
  - command: "/usr/local/bin/configure-jarvis.sh ${ROOT}"
    timeout: 60
