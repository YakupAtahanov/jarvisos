---
# Package installation configuration
# Additional packages to install during installation

backend: pacman

operations:
  - install:
    # JARVIS and dependencies are already in the SquashFS
    # This is for any additional packages needed after installation
    - base
    - base-devel
    - linux
    - linux-firmware







